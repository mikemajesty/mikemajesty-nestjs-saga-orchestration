{
  "name": "@app/payment.api",
  "scripts": {
    "docker:payment": "docker-compose up --no-deps --build payment-db zookeeper redpanda-console kafka payment-service",
    "build": "npx nest build payment-service"
  }
}