{
  "name": "@app/order.api",
  "scripts": {
    "docker:order": "docker-compose up --no-deps --build order-db zookeeper kafka redpanda-console order-service",
    "build": "nest build order-service"
  }
}
