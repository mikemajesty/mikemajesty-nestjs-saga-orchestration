{
  "name": "@app/orchestrator.api",
  "scripts": {
    "docker:orchestrator": "docker-compose up --no-deps --build zookeeper kafka redpanda-console orchestrator-service",
    "build": "nest build orchestrator"
  }
}
